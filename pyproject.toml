[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "edpop-explorer"
version = "0.0.1"
dependencies = [
  "sruthi"
]

[tool.pytest.ini_options]
pythonpath = [
  "edpop_explorer"
]
